// Desarrollar un algoritmo que evalué la siguiente expresión aritmética 1/n.
if (n === 1){
let n = parseInt(prompt("Digite un numero N: "))
let resultado = (1/n)
console.log("Su resultado es : ", resultado)
}else{
    console.log("Digite un numero mayor de 0")
}